- name: Create a k8s namespace
  kubernetes.core.k8s:
    name: "{{ nextcloud_name }}"
    api_version: v1
    kind: Namespace
    state: present
  when: k3s_first_cn and nextcloud_state == "present"

- name: Provision postgresql db server
  kubernetes.core.k8s:
    state: present
    namespace: "{{ nextcloud_namespace }}"
    template: 'pgsql.server.j2'
  when: k3s_first_cn and nextcloud_state == "present"

- name: (Cleanup) Deprovision postgresql db server
  kubernetes.core.k8s:
    state: absent
    namespace: "{{ nextcloud_namespace }}"
    template: 'pgsql.server.j2'
  when: k3s_first_cn and nextcloud_state == "absent"

